const mysql = require('mysql');
//database settings
const connection = mysql.createConnection({
  host     : 'localhost',
  user     : 'root',
  password : '*9sjE$xmc4#f%Q75za^yAPaCE',
  database : 'authentication'
});

//coonect to database
connection.connect(function(err) {
    if (err) throw err;
    console.log("Connected!");
});

//exports 
module.exports = {connection};